<h1>Adoptame</h1>
<div id="formwrapper">
    <form action="post">
        <select name="gender">
            <option>GÉNERO</option>
        </select>
        <select name="age">
            <option>EDAD</option>
        </select>
        <select name="breed">
            <option>TAMAÑO</option>
        </select>
        <input name="submit" type="button" value="Filtrar" class="submitbutton" />
    </form>
</div><% @dogs.each do |dog| %>
<div class="entry">
    <h2><a name="<%=h dog.name %>"></a><%=h dog.name %></h2>
    <table>
        <tr>
            <td>
                <a href="/images/<%= dog.picture_file_name %>" rel="lightbox" title="<%= dog.name %>"><%= image_tag dog.picture_file_name_list, :width => '100', :class => 'alignleft', :alt => dog.name %></a>
            </td>
            <td valign="top">
                <p>
                    <%=h dog.description %>
                </p>
            </td>
        </tr>
    </table>
    <p class="posted">
    <strong>GÉNERO: </strong><%=h dog.long_gender %>
	<span>|</span>
	<strong>EDAD: </strong><%=h dog.age %>
	<span>|</span>
	<strong>TAMAÑO: </strong><%=h dog.long_size %>
	<span>|</span>
    Posted on <%=h dog.created_at.strftime("%m/%d/%Y") %>
</div>
<% end %>
<% content_for :sidebar do %>
    <r:snippet name="adoption_sidebar" />    
<% end %>
